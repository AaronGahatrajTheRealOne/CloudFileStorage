<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Home</title>
</head>
<body>
    <div class="container">
        <% if(files && files.length > 0){ %>
            <% files.forEach((element) =>{ %>
                 <img src="<%= element.secureURL %>" alt ="Image Error">
            <% }) %>
        <% } else{ %>
            <p>No Files has been Uploaded yet</p>
            <% } %>
    </div>
    <script>
        
    </script>
</body>
</html>